# 배포 가이드 — 공유 링크 만들기 (Supabase + Vercel)

이 문서 하나만 따라 하면, 링크를 보내면 누구나 접속하고 같은 데이터를 보는 홈페이지가 완성됩니다.
순서대로 진행하세요. 코딩 지식은 필요 없습니다.

전체 흐름: **① Supabase 만들기 → ② 표(테이블) 만들기 → ③ 접속키 복사 → ④ Vercel 배포 → ⑤ 키 등록**

소요 시간: 약 15~20분.

---

## ① Supabase 프로젝트 만들기 (5분)

1. https://supabase.com 접속 → **Start your project** → GitHub 또는 이메일로 가입
2. **New project** 클릭
3. 입력:
   - Name: `soulcrane-dashboard` (아무 이름이나 가능)
   - Database Password: **강력한 비밀번호 입력 후 어딘가 저장** (나중에 필요할 수 있음)
   - Region: `Northeast Asia (Seoul)` 선택 (한국에서 가장 빠름)
4. **Create new project** → 1~2분 기다리면 완성

---

## ② 데이터 표(테이블) 만들기 (3분)

1. 왼쪽 메뉴에서 **SQL Editor** 클릭
2. **New query** 클릭
3. 프로젝트에 들어 있는 `supabase_schema.sql` 파일을 열어 **전체 복사**
4. SQL 편집기에 붙여넣기 → 오른쪽 아래 **Run** 클릭
5. "Success. No rows returned" 이 뜨면 완료

> 이 단계에서 videos / weekly_metrics / platform_followers 세 개의 표와 접근 권한이 만들어집니다.
> 데이터를 직접 넣을 필요는 없습니다 — 앱이 처음 켜질 때 자동으로 채웁니다.

---

## ③ 접속 정보(키) 복사 (1분)

1. 왼쪽 메뉴 맨 아래 **Project Settings**(톱니바퀴) → **API** 클릭
2. 두 값을 복사해 둡니다:
   - **Project URL** — `https://xxxxxxxx.supabase.co` 형태
   - **API Key** — 아래 둘 중 하나 (프로젝트 생성 시기에 따라 화면이 다릅니다):
     - 최신 화면: **Publishable key** (`sb_publishable_...`)
     - 이전 화면: **anon / public** (`eyJhbGci...`)
   - **둘 다 사용 가능합니다.** 앱이 두 형식을 모두 지원합니다.

> ⚠️ `service_role`(또는 `secret`) 키는 절대 쓰지 마세요. 외부에 노출되면 DB 전체가 위험합니다.
> 반드시 **Publishable / anon / public** 키를 씁니다.

---

## ④ Vercel에 배포하기 (5분)

### 방법 A — GitHub 연동 (권장, 이후 자동 업데이트됨)

1. 이 프로젝트 폴더를 GitHub 저장소로 올립니다.
   (GitHub Desktop 앱을 쓰면 클릭 몇 번으로 가능)
2. https://vercel.com 접속 → GitHub로 가입/로그인
3. **Add New → Project** → 방금 올린 저장소 선택 → **Import**
4. 설정 화면에서 Framework Preset이 **Vite** 로 자동 인식되는지 확인 (아니면 수동 선택)
5. **환경 변수(Environment Variables)** 펼치고 아래 2개 추가:

   | Name | Value |
   |------|-------|
   | `VITE_SUPABASE_URL` | ③에서 복사한 Project URL |
   | `VITE_SUPABASE_ANON_KEY` | ③에서 복사한 anon 키 |

6. **Deploy** 클릭 → 1~2분 후 `https://프로젝트이름.vercel.app` 주소가 나옵니다.

### 방법 B — Vercel CLI (터미널 사용)

```bash
npm i -g vercel      # 최초 1회
vercel               # 로그인 후 안내 따라 진행
vercel env add VITE_SUPABASE_URL          # ③의 URL 붙여넣기
vercel env add VITE_SUPABASE_ANON_KEY     # ③의 anon 키 붙여넣기
vercel --prod        # 실제 배포
```

---

## ⑤ 완료 확인

1. 나온 `https://....vercel.app` 주소를 엽니다.
2. 왼쪽 사이드바 하단에 **"공유 DB 연결됨"** (초록색)이 보이면 성공입니다.
   - 만약 "로컬 저장"이라고 뜨면 → 환경 변수가 안 들어간 것. ④의 5번을 다시 확인하고 재배포.
3. 이 주소를 팀원에게 보내면, 누구나 접속해서 같은 데이터를 보고 입력할 수 있습니다.

---

## 기존에 입력한 데이터는 어떻게 되나요?

**자동으로 옮겨집니다.** 그동안 본인 브라우저에 입력해 둔 데이터가 있으면,
Supabase 연결 후 그 브라우저에서 사이트를 처음 열 때 **1회 자동으로 DB에 업로드**됩니다.
(로컬 데이터는 지우지 않고 그대로 두므로, 문제가 생겨도 원본이 남아 있습니다.)

> 중요: 데이터를 입력했던 **바로 그 브라우저**에서 한 번 접속해야 이전이 됩니다.
> 여러 기기에서 입력했다면, 각 기기에서 한 번씩 사이트를 열어 주세요.

---

## 보안 안내

현재 구조는 "링크를 아는 사내 사람들이 함께 편집"하는 모델입니다.
로그인이 없으므로 **링크(및 사이트)가 외부에 공개되지 않도록** 관리하세요.
더 엄격한 접근 제어가 필요하면 Supabase 로그인(Auth) 추가를 요청해 주세요.

---

## 문제가 생기면

- **"데이터를 불러오는 중…"에서 멈춤 (무한 로딩)** → 이제 이 상태는 자동으로 풀리고, 화면 상단에 빨간 배너로 원인이 표시됩니다. 배너 내용에 따라 아래를 확인하세요:
  - "테이블이 없습니다" → ②의 `supabase_schema.sql` 실행을 안 했거나 일부만 실행함. **전체**를 다시 실행.
  - "권한이 거부됐습니다" → 스키마 파일의 **맨 아래 RLS 정책 부분까지** 실행됐는지 확인. SQL 전체를 다시 실행하면 됩니다.
  - "서버에 연결하지 못했습니다" → `VITE_SUPABASE_URL` 주소 오타 확인 (https:// 로 시작, 끝에 공백/슬래시 없이).
- **브라우저 콘솔(F12 → Console)** 에 `[Supabase] URL 감지: 있음/없음 · 키 감지: 있음/없음` 로그가 찍힙니다. 둘 중 "없음"이 있으면 환경변수 이름이나 위치 문제입니다.
- **환경변수가 안 읽힘** → ① `.env` 파일이 `package.json`과 **같은 폴더(프로젝트 루트)**에 있는지, ② 변수 이름이 정확히 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`(또는 `VITE_SUPABASE_PUBLISHABLE_KEY`)인지, ③ **개발 서버를 완전히 껐다가 다시 켰는지**(`.env`는 서버 재시작 후에만 반영됨) 확인.
- **"로컬 저장"에서 안 바뀜** → 위 세 가지를 확인 후 `npm run dev` 재시작.
- **데이터가 안 보임** → Supabase SQL Editor에서 `select count(*) from videos;` 실행해 62가 나오는지 확인.
- **새로고침 시 화면이 깨짐** → `vercel.json`이 프로젝트에 포함됐는지 확인 (SPA 라우팅 설정).
